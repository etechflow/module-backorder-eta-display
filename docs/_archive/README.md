# Archived pre-v1.1.0 install guides

These files describe the original v1.0.x module before the v1.1.0 UX overhaul
(module status banner, expanded tooltips, grid column + filter).

Kept here for reference only — not shipped in the dist zip. Current install
instructions live in `../README.md` and `../USER_GUIDE.md`.
